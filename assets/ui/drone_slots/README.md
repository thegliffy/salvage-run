# Drone bay slot chips

Painted chips for combat drone bays (`drone_slot_empty`, `drone_slot_attack`,
`drone_slot_shield`). Combat loads them through `UITheme.drone_slot_texture()`
onto TextureRects in the header; missing files fall back to StyleBox rings.
