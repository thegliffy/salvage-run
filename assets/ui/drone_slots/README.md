# Drone bay slot chips

Optional painted chips for combat drone bays (`drone_slot_empty`, `drone_slot_attack`, `drone_slot_shield`).
Load via `UITheme.art("res://assets/ui/drone_slots/<name>.png")` when wiring TextureRects over the StyleBox rings.
